
spring:
  datasource:
    url: jdbc:mysql://service-database:3306/test?autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true
    username: potato
    password: potato123
    hikari:
      initialization-fail-timeout: 0

  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect


server:
  port: 8082

